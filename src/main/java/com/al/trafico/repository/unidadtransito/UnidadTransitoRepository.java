package com.al.trafico.repository.unidadtransito;

import com.al.trafico.entity.UnidadTransito;
import org.springframework.data.jpa.repository.JpaRepository;

public interface UnidadTransitoRepository extends JpaRepository<UnidadTransito, Long> {
}